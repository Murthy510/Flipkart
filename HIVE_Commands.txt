HIVE QUERIES:

connect hive in cloudera:

cd /usr/lib
CREATE DATABASE IF NOT EXISTS APEX;
SHOW DATABASES;
CREATE DATABASE IF NOT EXISTS MIMS;
DROP DATABASE IF EXISTS MIMS;
SHOW DATABASES;

CREATE TABLE IF NOT EXISTS APEX.EMPLOYEE
(EMP_NO INT, EMP_NAME STRING, EMP_LOC STRING)
COMMENT 'EMPLOYYE DETAILS'
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\T'
LINES TERMINATED BY '\N'
STORED AS TEXT FILE;



Error while compiling statement: FAILED: ParseException line 7:15 extraneous input 'FILE' expecting EOF near '<EOF>'

CREATE TABLE IF NOT EXISTS APEX.EMPLOYEE
(EMP_NO INT, EMP_NAME STRING, EMP_LOC STRING)
COMMENT 'EMPLOYYE DETAILS'
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE;

load data local inpath '/home/cloudera/murthy/sample.txt' overwrite into table student;

DROP DATABASE MIMS CASCADE;


CREATE TABLE IF NOT EXISTS APEX.Student
(EMP_NO INT, EMP_NAME STRING, EMP_LOC STRING)
COMMENT 'EMPLOYYE DETAILS'
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE;